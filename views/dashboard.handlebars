<h2 class="title">dashboard: your trails</h2><hr>
{{#if trails.length}}
    <div>
    {{#each trails as |trail|}}
        {{> trail-info trail}}
    {{/each}}
    </div>
{{else}}
<h3>you do not have any trail yet</h3>

{{/if}}
