<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.name}}
    </div>
    <div class="text">
      {{comment}}
    </div>
  </section>
  {{/each}}
</div>
<script src="javascript/comment.js"></script>
