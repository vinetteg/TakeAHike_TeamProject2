<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.name}} on {{format_date created_date}}
    </div>
    <div class="text">
      {{comment}}
    </div>
  </section>
  {{/each}}
</div>
<script src="javascript/comment.js"></script>
